import { DangerLabelDirective } from './danger-label.directive';

describe('DangerLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new DangerLabelDirective();
    expect(directive).toBeTruthy();
  });
});
